{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "node",
        "request": "launch",
        "name": "Launch Program",
        "program": "${workspaceFolder}/src/main.ts", // Change this to your entry file
        "outFiles": ["${workspaceFolder}/**/*.ts"]
      }
    ]
  }
  